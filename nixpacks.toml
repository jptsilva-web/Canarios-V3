[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "nodejs-18_x"]

[phases.install]
cmds = ["cd backend && pip3 install -r requirements.txt"]

[start]
cmd = "cd backend && python3 -m uvicorn server:app --host 0.0.0.0 --port ${PORT:-8000}"
