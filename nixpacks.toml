[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "nodejs-18_x"]

[phases.install]
cmds = ["cd backend && pip3 install --break-system-packages -r requirements.txt"]

[phases.start]
cmd = "cd /app/backend && python3 -m uvicorn server:app --host 0.0.0.0 --port ${PORT:-8000}"
